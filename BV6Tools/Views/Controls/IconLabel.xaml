<UserControl x:Class="BV6Tools.Views.Controls.IconLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BV6Tools.Views.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             x:Name="Root"
             d:DataContext="{d:DesignInstance local:IconLabel,
                                              IsDesignTimeCreatable=True}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel DataContext="{Binding RelativeSource={RelativeSource AncestorType=local:IconLabel}}"
                Orientation="{Binding Orientation}">
        <ui:SymbolIcon HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Filled="{Binding Filled}"
                       FontSize="{Binding SymbolFontSize}"
                       FontWeight="{Binding SymbolFontWeight}"
                       Symbol="{Binding Symbol}" />
        <ui:TextBlock HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      FontSize="{Binding LabelFontSize}"
                      FontWeight="{Binding LabelFontWeight}"
                      Text="{Binding Label}">
            <ui:TextBlock.Style>
                <Style TargetType="ui:TextBlock">
                    <Setter Property="Margin" Value="4,0,0,0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Orientation}"
                                     Value="Vertical">
                            <Setter Property="Margin" Value="0,4,0,0" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ui:TextBlock.Style>
        </ui:TextBlock>
    </StackPanel>
</UserControl>
